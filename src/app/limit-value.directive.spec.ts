import { LimitValueDirective } from './limit-value.directive';

describe('LimitValueDirective', () => {
  it('should create an instance', () => {
    const directive = new LimitValueDirective();
    expect(directive).toBeTruthy();
  });
});
